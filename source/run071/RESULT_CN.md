# Run71 原始 EEG 因果状态缓存结果

## 机器结论

状态：`complete`。本轮没有训练模型，没有读取目标曲线数值，没有按结果挑事件，也没有读取 strict/offline FIF 或未来 QC。

## 事件与特征

- 事件：`2323`，唯一 event_uid：`2323`。
- 主窗活跃：`2182`；移位窗活跃：`2147`。
- 原始 EEG 可映射事件：`2185`；不可用但保留：`138`。
- 每窗信号特征：`46`（频谱 24 + 协方差 22）；主窗质量特征：`11`。

## 录制层审计

- 成功顺序处理录制：`79`；自然缺失：`6`；格式/时间/滤波条件不满足但保留事件：`0`。
- 原始第一时间戳相对 manifest 起点超出 ±100 ms 的录制数：`0`。该指标只报告，脚本没有按事件平移时间。
- 有限主窗支持的最大 `support - prediction_anchor`：`0.0` s；越过 `1e-6 s` 的事件数：`0`。

## 边界

这是一个因果原始 EEG 状态缓存及可用性事实层，不是 EEG 能改善未来方向盘曲线预测的证据。当前没有拟合切空间参考、PCA 或模型；后续如使用这些变换，必须只在训练折拟合。
