# Run58 部署前代码复核

复核范围：`config.json`、`experiment.py`、`validate.py`、Run58 brief，以及两项允许的输入接口。
未运行实验，未修改代码，未接触禁止的独立检查脚本。

## 第一轮发现并修复

- 分档表补充18名被试等权指标，同时保留用于复现旧数值的事件pooled指标。
- 验证器改为按 `event_uid` 从ledger独立读取20点真实目标。
- 验证器从逐事件样本外预测重算分档结果。
- 验证器按 `20260827 + fold` 独立重建内层验证划分。
- 扩大锚点失败后的停止边界检查。
- 人口禁止字段检查不再直接写死为通过。
- 增加172维缓存的形状、列名和事件行序检查。

## 第二轮发现并修复

- 预测UID必须与完整样本清单有序一致，并等于独立人口规则得到的UID集合。
- 被试、外层折、释放幅值和20点目标全部按UID从ledger独立对齐。
- 全部实际存在的幅值档均独立复算事件数、被试数、pooled/被试等权MAE、幅值中位数和两套相对误差。
- 候选索引与ExtraTrees候选组合、内验随机种子和最终随机种子逐项闭合。

## 最终结论

- 阻断问题：无。
- 第3.1节ExtraTrees复现锚点：可以正式运行。
- 锚点失败后的立即停止边界：已闭合。
