# Run58 已停止：ExtraTrees复现锚点未达到容差

## 当前状态

**BLOCKED。** 第3.1节复现锚点没有全部通过，已按合同立即停止。
道路预览地图、15维预览特征、保持角度对照、预览增量模型和配对bootstrap均未运行。

## 锚点结果

- 指定的 pooled 事件MAE目标：`13.0214°`
- 实际 pooled 事件MAE：`13.609839°`
- 同时记录的18名被试等权MAE：`13.000393°`

- `pooled_event_mae`：失败；观测 `13.609839`，目标 `13.021400`，差值 `+0.588439`，容差 `±0.001000`。
- `relative_error_20_30`：通过；观测 `0.349476`，目标 `0.348000`，差值 `+0.001476`，容差 `±0.005000`。
- `relative_error_30_45`：通过；观测 `0.360982`，目标 `0.362000`，差值 `-0.001018`，容差 `±0.005000`。
- `relative_error_45_70`：通过；观测 `0.369923`，目标 `0.371000`，差值 `-0.001077`，容差 `±0.005000`。
- `relative_error_ge70`：失败；观测 `0.355067`，目标 `0.349000`，差值 `+0.006067`，容差 `±0.005000`。

## 为什么停止

合同要求任一锚点超出容差就停止，不允许继续抽取道路预览、调参或改门槛。
当前结果只说明复现锚点没有闭合；不能据此评价道路预览是否有效。

## 完整性

- 使用指定环境：`<PYTHON_ENV>\python.exe`。
- 两项不可变输入SHA-256已匹配。
- 样本2323、18名被试、85个记录和五折计数均复现。
- 199个道路参考缺失事件未删除。
- raw输入支持未超过release。
- 外部契约检查在本轮内没有执行；`G-P0-0`记录为`EXTERNAL`。
- 没有运行或接触被禁止的独立检查脚本。
