# 项目背景与研究问题

## 1. 研究动机

本项目研究车辆高动态、失控或近失稳代理场景中，驾驶员在车辆状态快速变化后会如何操纵方向盘。

核心关注不是一般车道保持，也不是只判断“是否危险”，而是：

> 在一个明确的预测起点，利用当时及以前能够获得的车辆、道路和驾驶员状态，预测驾驶员随后约1秒的完整转向响应曲线。

完整曲线比单一类别或峰值更接近真实控制过程，因为驾驶员可能经历：

- 继续增加转向；
- 短暂保持；
- 快速回舵；
- 反向修正；
- 多阶段振荡；
- 尾部继续增长或衰减。

## 2. 科学边界

数据来自模拟驾驶与车辆动力学记录。当前“失稳/风险”是基于车辆高动态、方向盘快速输入和车辆响应构造的代理事件，不是轮胎离地、真实侧翻、LTR、TTR或事故真值。

因此论文可以讨论：

- 高动态/近失稳代理事件；
- 驾驶员转向响应；
- release-time跨被试预测；
- 不同信息模态的增量价值。

不能直接声称：

- 真实事故预测；
- 理论不可约驾驶员噪声；
- 驾驶行为本质随机；
- 某数值是因果噪声下限。

## 3. 两批实验数据

### 原始主队列

- 18位驾驶员；
- 85个主要recordings；
- 当前严格因果P_full为2323个事件；
- 包含车辆、道路、生理、EEG和部分眼动材料；
- Run57以后是当前主要模型合同。

### 2025年8月补充队列

- 当前恢复出29个被试目录；
- 26位被试具有合格车辆事件；
- 27位具有四通道PhysioLAB生理记录；
- 275个事件满足当前筛选合同；
- 相对原18人增加20位真正新的驾驶员；
- 合并后为2598个事件、38位驾驶员、190个recordings。

八月实验与原主队列不是完全相同的实验批次：

- 场景组合略有变化；
- 某些后段弯道/匝道场景减少；
- 通常一个文件对应一次场景运行；
- 缺少`vyaw/vroll`两个车辆rate字段；
- 速度和部分车辆信号分布与原队列不同。

研究重点仍是驾驶员反应与车辆状态，因此八月数据具有价值；但不能把路线ID或场景身份直接当成跨批次可交换信息。Run76已证明直接合池会伤害原队列，必须使用被试/域均衡或把八月作为独立域报告。

## 4. 事件与预测起点

当前事件由Anchor-v3类规则从连续车辆记录中检测。事件成员条件关注：

- release前历史完整；
- release前速度满足条件；
- 无倒车；
- 方向一致性；
- 车辆输入覆盖率；
- release后1秒目标完整只作为离线标签质量检查。

道路有效性和未来响应幅值不是事件成员门。

预测起点为方向盘快速输入结束/释放附近的`release`。模型输入必须停止在release，不能使用release后的方向盘、车辆、生理或道路观测。

## 5. 预测目标

目标为release后：

```text
+0.05, +0.10, ..., +1.00 seconds
```

共20个点。

方向对齐定义：

```text
aligned_deg = degrees((target - release_value) * direction)
```

其中：

- `release_value`是预测起点方向盘值；
- `direction`把左右事件统一为同一逻辑方向；
- 目标是release后的增量转向曲线，不是绝对方向盘位置。

## 6. 输入信息

### 车辆与道路

旧主队列曾使用9个时序通道。为兼容八月数据，当前共同输入主要是：

- steering angle；
- steering rate；
- speed；
- lateral acceleration；
- roll；
- curvature；
- lateral distance；
- road-missing mask。

`vyaw/vroll`缺失消融显示其整体MAE影响很小，因此不阻断新增驾驶员使用。

### 生理

四通道PhysioLAB：

- ECG；
- EMG；
- EDA；
- RESP。

Run79已将八月188个recordings处理成通道专用清洗连续层和事件特征。Run80表明，正式清洗并没有让生理直接拼接成为有效均值预测增量。

### EEG与眼动

- 原主队列具有较广的release前EEG支持；Run71完成因果缓存，Run72没有得到独立模型增量。
- 眼动数据在部分原recordings可用；Run73没有形成车辆基线之上的稳定增量。

这些模态仍可用于机制分析，但目前不是主预测器的已验证增量。

## 7. 评价协议

主要原则：

- subject-disjoint外折；
- 同一驾驶员所有session在同一fold；
- 训练折拟合缺失中位数、标准化和模型选择；
- 测试被试不进入训练、早停或候选选择；
- 被试级配对bootstrap；
- 主指标为subject-macro；
- pooled只作参考。

主要报告：

- 完整20点曲线MAE；
- head5、tail5、endpoint；
- peak-time误差；
- 按幅值档的relative MAE；
- 改善/退化被试数；
- 普通样本伤害；
- 道路缺失和数据域分层。

## 8. 当前模型演化

### 静态摘要模型

Run57建立当前严格因果基线。ExtraTrees在原18人上长期保持强势。Run60说明LightGBM和HistGBM在同一静态摘要上没有稳定超过ExtraTrees。

### 结构化输出、残差与门控

Run62的幅值—形状因子化、Run63的低秩残差与软门控都没有达到冻结晋级门。

### 多模态

Run64–Run68、Run72、Run73和Run80分别检验生理、驾驶风格、教师学生、耦合、区间、EEG与眼动；没有一条直接均值预测路线稳定晋级。

### 新驾驶员

Run75说明缺失rate字段不是主要阻碍。Run76说明直接加入早期八月数据有害。Run78形成275事件新队列，Run79完成生理处理。

### 原始序列模型

Run81把LGRS关系状态方法细化到可执行合同。Run82显示：

- Role-TCN优于Plain Raw-TCN；
- LGRS稳定优于参数配平Role-TCN；
- lag扰动支持模型确实使用指令—响应时序；
- 但LGRS仍显著落后ExtraTrees。

因此关系机制有证据，但神经序列模型不能取代当前树模型。

## 9. 当前真正的研究问题

现阶段不应继续问“换哪个通用模型”。更准确的问题是：

1. ExtraTrees强势来自小样本归纳偏置、静态摘要质量，还是当前target的条件中位数特性？
2. Run82的关系状态能否转化为少量合法、训练侧冻结的树模型输入，而不重复Run62手工相位特征？
3. 新增20位驾驶员应作为域适配、顺序个体化还是外部泛化材料使用？
4. 还有哪一种release时可用的新信息没有被现有no-go覆盖？
5. 是否应承认release-time任务的信息限制，并另立一个等待50–400 ms后的changed-estimand任务？

GPTPro的下一步计划必须在这些问题中只选择一条最有证据的主线，不能把所有方向重新组合成大系统。

